"""Spatial matching placeholders for routed edges and observed counts."""

from __future__ import annotations


def describe_spatial_matching() -> str:
    return (
        "Spatial matching will support nearest-neighbor and buffer-overlap matching "
        "with distance, route-name, angle, and functional-class diagnostics."
    )
